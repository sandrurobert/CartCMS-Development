<?xml version="1.0"?>
<title>Organizational rules</title>

<models>
  <rule>Models only include functions for database queries</rule>
</models>

<lang>
  <rule>lang variables names from controllers should only start with lang_</rule>
  <rule>arrays should be organized nicely per group</rule>
  <rule>we have the following files for every language: [language name], error, misc</rule>
</lang>

<?xml version="1.0"?>
<title>TODO list</title>

<high_priority>
</high_priority>


<backend>

  <pages>
    <action>add empty page type, value to 0</action>
    <action>add template parts</action>
  </pages>

  <config>
    <action>create config file for cms</action>
  </config>

  <errors>
    <action>add error pages</action>
  </errors>

  <users>
    <action>Create add new user</action>
    <action>change to ion-auth</action>
  </users>

  <sidebars>
    <action>edit sidebars content</action>
  </sidebars>

  <settings>
    <action>fix upload website logo</action>
  </settings>

</backend>


<frontend>

  <lang>
    <action>lang to frontend</action>
  </lang>

  <templates>
    <action>add fallback template parts</action>
  </templates>

</frontend>


<both>

  <migrations>
    <action>set up migrations</action>
    <action>create migrations for base structure</action>
  </migrations>

  <queries>
    <action>changes queries by CI actions</action>
  </queries>

  <modules>
    <action>install modularity & use it</action>
  </modules>

</both>